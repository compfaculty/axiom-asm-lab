# Evidence: T010 (revalidated)

## Change and acceptance mapping
- Task: T010 — Expand kernels through the shared verify + paired measure pipeline (M005).
- Requirement: ≥3 kernel families with contracts/oracles; wrong/bounds/ABI faults block timing; observation vs inference distinguished; no mandatory speedup.
- Implementation: `kernels/descriptors.py`, `src/harness_find.c`, `src/harness_map_filter.c`, `src/abi_wrap_find.s`, `src/abi_wrap_map_filter.s`, `asm/find_u8.s`, `asm/map_filter_u64.s`, `candidates/wrong_find.s`, `candidates/wrong_map_filter.s`, `lab.py` (`compile_one`/`oracle_check`/`kernel-eval`), `evaluate.py`, `proposals.py`, `kernels/experiments.py`, `tests/test_kernel_pipeline.py`.

## Environment and provenance
- OS: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Host platform: `macOS-26.6.2-arm64-arm-64bit-Mach-O`
- SHA-256 (post-change):
  - `kernels/descriptors.py`: `5b344b87a952417fa466a361f27d3c42107e76eaa571b53679e77336406fb5e6`
  - `lab.py`: `3196924d9bd451c97626083e3d9ca6baf06c970e06988ce9479f409f2300067f`
  - `evaluate.py`: `11c546ada3b3b4beb08ae29241135015426e3dcd0dcf05db4957db1cba3c8bc2`

## Executed checks

| Command | Exit | Result |
|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 134 OK |
| `python3 lab.py kernel-eval --samples 3 --seed 1 --output results/kernel_eval_t010.json` | 0 | find_u8, map_filter_u64, sum_u64 verify+smoke PASS; wrong candidates blocked |
| `python3 lab.py portfolio --seed 1 --output results/portfolio_t010.json` | 0 | PASS three kernels |
| `python3 scripts/check_project.py` | 0 | PASS bookkeeping |

## Pipeline properties
- Per-kernel descriptors: harness, ABI wrap, baseline, sizes, oracle file command, wrong fixture.
- Shared path: `build_and_verify` → `measure_paired` (smoke) for all three.
- Wrong asm fixtures fail verify/oracle before timing.
- Evidence summaries: `evidence_summary.json` beside measurements (`sanitize_evidence_summary`).
- Experiments schema 2 adds `controlled_paired_smoke` protocol with explicit assumptions.

## Limitations
- Search/catalog ranking remains sum_u64-default (proposals allow all three; catalog entries still sum).
- Smoke measurements are non-promotional; no speed claim.
- Cache labels remain footprint heuristics (documented).

## Acceptance
M005 / T010 complete via shared evaluate pipeline (not portfolio-only).
