# Evidence: T011 (revalidated)

## Change and acceptance mapping
- Task: T011 — Semantic IR and interpreter, revalidated after T010 descriptor wiring.
- Requirement: Typed ops/effects; interpreter matches oracles; ill-typed/unsupported rejected; semantics documented.
- Files: `language/ir.py`, `language/interpreter.py`, `language/SEMANTICS.md` (unchanged semantics); reconnect via descriptor identity in lowering (T012).

## Environment
- macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0

## Executed checks

| Command | Exit | Result |
|---|---|---|
| `python3 -m unittest tests.test_language_ir -v` | 0 | IR interpreter cases OK (suite in full discover) |
| `python3 -m unittest discover -s tests -v` | 0 | 134 OK |
| `python3 lab.py lang-check` | 0 | PASS n=3 |

## Notes
- Interpreter remains the trusted oracle for `.ax` programs.
- Kernel identities for sum/find/map_filter align with `kernels.descriptors` symbols after T010.
- No Stage 4 ownership/concurrency features claimed.

## Acceptance
T011 revalidated against current code and three verified kernel families.
