# Evidence: T003

## Change and acceptance mapping
- Task: T003 — Define contract-driven test generation (M002)
- Requirement/check: Kernel metadata + independent wrapping oracle; seeded distributions and boundary lengths; zero/null semantics; built-ins match oracle; intentionally wrong sums fail; seed reproduces failure; overflow and unroll boundaries covered.
- Implementation files: `kernels/sum_u64.py`, `kernels/__init__.py`, `src/harness.c` (`sum-file`), `lab.py` (`oracle_check`), `candidates/wrong_sum.s`, `tests/test_sum_oracle.py`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git parent: `9ba5bce` (T002)
- SHA-256 at evidence time:
  - `lab.py`: `66ddf21a487aa839d23241575f7f990d481f73ba118d1310e4ce43a1c9c7640c`
  - `src/harness.c`: `857d56e9a97bec78432e44337f0ce14f73d5558946b05d897ecbf63a6b5f0d1d`
  - `kernels/sum_u64.py`: `5b8eea096194e3ff17875c6cf8df28ed6c7d6702bbf26f67e5ab5e7a44aaf3e9`
  - `candidates/wrong_sum.s`: `e1b2c287e0374ecb165609f628ae7dbd2725d67e58716b9dc4d2ad11fbf597eb`
- Oracle run: `build/runs/20260923T222719_5fe54f3f` — 115 cases, fingerprint `8dff6b338927416d…`, all built-ins `oracle.state=pass`

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 20 tests OK including oracle/seed/wrong-sum/boundaries | stdout |
| `python3 lab.py verify` | 0 | Built-ins PASS harness verify + oracle differential (`oracle.state=pass`) | `build/runs/*/manifest.json` |
| `python3 lab.py verify --candidate wrong_sum` | 1 | Rejected: `FAIL empty n=0 got=1 expected=0` | stderr |

## Failure tests
- Portable `wrong_sum()` diverges from `oracle()`; first failing label stable under seed replay.
- Native `candidates/wrong_sum.s` fails built-in verify (returns 1 for empty).
- Overflow wrapping asserted (`UINT64_MAX+1 -> 0`).

## Limitations
- Guard-page / ABI fault fixtures remain T004.
- Oracle case set is seeded and bounded; not an exhaustive fuzzer.

## Decision and next action
Pass.

Exact next command: begin T004 — native memory and ABI checks.
