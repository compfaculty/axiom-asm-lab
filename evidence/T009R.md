# Evidence: T009R

## Change and acceptance mapping
- Task: T009R — Validate review fixes (confirmation storage, budget propagation, resume identity) on macOS arm64.
- Plan: `docs/NEXT_CURSOR_PLAN.md` §T009R.
- No feature changes in this task; native revalidation only.

## Environment and provenance
- OS: macOS 26.6.2 (Build 25G83), arm64
- Python: 3.14.7
- Clang: Apple clang version 21.0.0 (clang-2100.3.34.2)
- Git: `7f7915b75ac4006f1b50a4c24a95668a6148a91f`
- Host platform string: `macOS-26.6.2-arm64-arm-64bit-Mach-O`

## Executed checks

| Command | Exit | Result |
|---|---|---|
| `python3 scripts/check_project.py` | 0 | PASS bookkeeping |
| `python3 -m unittest discover -s tests -v` | 0 | 116 OK (native tests executed; none skipped for platform) |
| `python3 lab.py verify` | 0 | clang_o3 / scalar / unrolled4 PASS; run `20260925T203137_6d24568b` |
| `python3 lab.py fault-matrix` | 0 | 7/7 expected classifications; `20260925T203139_8e558178` |
| `python3 lab.py search --dir build/searches/review-smoke --smoke --provider catalog --max-proposals 2 --samples 3` | 0 | attempts=2; accepted=null; stop=max_proposals |
| `python3 lab.py search --dir build/searches/review-protocol --provider catalog --max-proposals 2 --samples 30 --seed 1` | 0 | attempts=2; best_observed=catalog_unrolled4≈0.91 (regression); accepted=null |
| `python3 lab.py search --dir build/searches/review-protocol --resume --provider catalog --max-proposals 5 --samples 30 --seed 1` | 0 | attempts extended 2→5; best_observed=catalog_unrolled8≈0.92 (regression); accepted=null |

## Confirmation / sample isolation
- No candidate was promote-eligible, so no confirmation sessions ran (valid outcome).
- Each ranked attempt has its own `primary/samples.jsonl` and `primary/measurement.json` under distinct attempt directories:
  - `0000_…` … `0004_…` (5 primary sample files after resume).
- Deterministic confirmation-path isolation remains covered by portable test `test_confirmation_uses_distinct_sample_files_and_remaining_budget`.

## Resume identity
- Resume with unchanged code, seed, provider, and sampling configuration succeeded.
- `search_state.json` retains `resume_identity` (source-tree file hashes) and `evaluation_config`.

## Limitations
- No speed claim; all catalog candidates classified as regression vs clang_o3.
- Confirmation storage not exercised natively in this run (no promote-eligible candidate); covered by synthetic integration test.
- CPU brand string via `sysctl` was unavailable in the recording shell; machine=`arm64` and platform string recorded.

## Acceptance
- Portable + native suite green on target Mac.
- Smoke and full-protocol searches in **new** directories succeed; resume extends attempts.
- T009R complete; next: T009H recovery/evidence hardening.
