# Evidence: T002

## Change and acceptance mapping
- Task: T002 — Make bootstrap artifacts traceable (M001)
- Requirement/check: Capture complete build argv, compiler identity, source/harness/binary hashes and verification state; prevent candidate ID collisions with built-ins; unique run directories; source/harness edit invalidates previous verification; runs never overwrite each other; malformed names and missing files fail clearly (`docs/IMPLEMENTATION_PLAN.md`).
- Implementation files: `lab.py` (run dirs, manifests, freshness checks, overwrite refusal, builtin collision); `tests/test_controller.py` (portable acceptance coverage).

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2, arm64, Python 3.14.7, Apple clang version 21.0.0 (clang-2100.3.34.2)
- Revision: git parent `63b374f` (T001 baseline); working tree includes this T002 change
- Source hashes at evidence time:
  - `lab.py`: `bcc4530154cd268a1f1e8f7c867c11e9cd310585b6eb45d5966bb30d1ce16ea4`
  - `src/harness.c`: `b4fcf68adf984e9cc488e79e532d0daf75d170cfca0619ef59b053a3e5c2dca5` (unchanged from T001)

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 12 tests OK (collision, missing file, unique dirs, overwrite, source/harness stale invalidation) | stdout |
| `python3 lab.py doctor` | 0 | Supported host reported | stdout |
| `python3 lab.py verify` | 0 | Three built-ins PASS; unique `build/runs/<id>/manifest.json` with argv + hashes + `verification.state=pass` | e.g. `build/runs/*/manifest.json` |
| `python3 lab.py bench --samples 15` | 0 | Unique `results/runs/<id>/bench.json` + matching run manifest | `results/runs/*/bench.json` |
| `python3 lab.py bench --samples 15 --output results/runs/should_collide.json` (second time) | 1 | `ERROR: Refusing to overwrite existing artifact` before remeasure | stdout/stderr |
| Candidate `scalar` / missing `nope` | raised | Collision and FileNotFoundError | Python API |

## Failure tests
- Builtin name collision rejected (`clang_o3`, `scalar`, `unrolled4`).
- Malformed candidate stems and path traversal rejected (`ValueError`).
- Missing `candidates/<stem>.s` raises `FileNotFoundError`.
- `ensure_fresh` marks verification `stale` and raises if source or harness bytes change after build/verify.
- Existing bench output path refused (`FileExistsError` / CLI exit 1).

## Limitations
- Freshness is enforced for artifacts within a run and before measurement; full lifecycle persistence across process restarts for resume is T005/T009 scope.
- Empty directories under `build/runs/` may remain from unit tests allocating unique dirs; they are not manifests.
- Bench timings remain observational (no M003 promotion).

## Decision and next action
Pass. M001 bootstrap gate bookkeeping complete for T001+T002.

Exact next command: begin T003 — contract-driven test generation (`docs/IMPLEMENTATION_PLAN.md`).
