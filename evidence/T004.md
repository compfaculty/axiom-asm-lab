# Evidence: T004

## Change and acceptance mapping
- Task: T004 — Add native memory and ABI checks (M002)
- Requirement/check: Guarded mappings, read-only inputs, ABI sentinel wrapper; fixtures in child processes with signal/timeout classification. Overread, underread, input-write, callee-saved corruption, trap, and infinite-loop rejected for intended reasons; correct fixtures pass.
- Implementation files: `src/harness.c` (mmap guards, RO, probe/probe-tight/probe-head), `src/abi_wrap.s`, `lab.py` (`fault-matrix`, `classify_child`), `fixtures/*.s`, `tests/test_fault_classify.py`, `docs/TEST_FLOW.md`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git parent: `681d232`
- SHA-256 at evidence time:
  - `src/harness.c`: `3d0d0b92b433675b302ed5e306c05ef69aed2f142ad00d6478dbd33846ed99fc`
  - `src/abi_wrap.s`: `647a355baaabed757b67207a66528b2ec842bc00c3129825c1855250820b2d1b`
  - `lab.py`: `d614427f865869e8a7be26167701f13924bf3d40a45ad0cd76c0acb8320b73d6`
- Fault matrix artifact: `build/runs/20260923T223504_89cbfba6/fault_matrix.json`
## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 27 tests OK | stdout |
| `python3 lab.py verify` | 0 | clang_o3/scalar/unrolled4 PASS (oracle + centered RO/ABI probe) | run manifest |
| `python3 lab.py fault-matrix` | 0 | All 7 rows OK (pass / memory_fault / abi_fail / trap / timed_out) | `build/runs/*/fault_matrix.json` |

Fault matrix (native):
- scalar → pass
- overread → memory_fault (`probe-tight`)
- underread → memory_fault (`probe-head`)
- input_write → memory_fault
- abi_corrupt → abi_fail
- trap → trap
- infinite_loop → timed_out

## Failure tests
Deliberate fixtures under `fixtures/` exercise each rejection class. Controller continues across child faults (matrix completes after crashes/timeouts).

## Limitations
- Correct-kernel `probe` centers the buffer with 256-byte readable slack so auto-vectorized Clang `-O3` is not false-faulted; tight/head surfaces remain for intentional over/under-read fixtures.
- Guard pages do not detect all in-range invalid accesses (documented in TEST_FLOW).

## Decision and next action
Pass.

Exact next command: begin T005 — enforce verification before timing / lifecycle states.
