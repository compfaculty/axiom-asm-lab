# Evidence: T009H

## Change and acceptance mapping
- Task: T009H — Harden crash recovery, persisted evidence validation, incumbent replacement, concurrent controller handling.
- Plan: `docs/NEXT_CURSOR_PLAN.md` §T009H.
- Implementation: `search.py`, `evaluate.py`, `sampling.py`, `tests/test_search_harden.py`, `tests/test_search_review.py`.

### Acceptance bullets
| Requirement | Implementation |
|---|---|
| Active-eval checkpoints + conservative interrupt charging | `checkpoint_in_flight`, stage checkpoints in `native_evaluate_attempt`, `_charge_interrupted` |
| Revalidate measurement/hashes before reusing rankings | `validate_measurement_evidence`, `revalidate_persisted_rankings` on resume |
| Retry interrupted catalog entry | `next_proposal_index` + `retryable` / `superseded_by` |
| Exclusive search-dir lock | `SearchDirLock` (fcntl) wrapping `SearchController.run` |
| Incumbent replacement policy | `INCUMBENT_POLICY`, `beats_incumbent`, gated in `_update_ranking` |
| Session identity: CPU + normalized compiler | `host_session_identity`, nested `resume_identity`, `evaluation_config` / reports |

## Environment and provenance
- OS: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git at validation: `7f7915b75ac4006f1b50a4c24a95668a6148a91f` (working tree dirty with T009H changes)
- Search state schema: 3

## Executed checks

| Command | Exit | Result |
|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 127 OK |
| `python3 scripts/check_project.py` | 0 | PASS (before status update) |
| `python3 lab.py search --dir build/searches/harden-smoke --smoke --provider catalog --max-proposals 2 --samples 3` | 0 | attempts=2; schema=3; host+compiler in resume_identity; accepted=null |

## Synthetic tests (not native speed evidence)
- Concurrent lock rejected
- Corrupted / missing measurement rejected on resume
- Catalog index retried after confirming interrupt
- Incumbent preserved when challenger score is lower
- Interrupt at confirmation preserves incumbent and retries
- Conservative charge applied for incomplete stages
- Resume identity includes `cpu_brand` and `compiler_config`

## Limitations
- `cpu_brand` may fall back to `platform.processor()` (`arm`) when `sysctl` is unavailable in the recording environment; `machine=arm64` and clang identity remain authoritative.
- No speed claim; smoke search is non-promotional.
- Historical search dirs with schema-2 flat resume identity must start a new directory.

## Acceptance
T009H complete. Next: T010 per-kernel evaluate pipeline.
