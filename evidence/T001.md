# Evidence: T001

## Change and acceptance mapping
- Task: T001 — Establish the host and baseline (M001)
- Requirement/check: On macOS arm64 all three built-ins verify and produce a report with every configured size; portable tests pass; command evidence exists (`docs/IMPLEMENTATION_PLAN.md`, M001 gate in `docs/ACCEPTANCE.md`).
- Implementation files: `src/harness.c` (minimal fix: `#define _DARWIN_C_SOURCE` so `CLOCK_MONOTONIC_RAW` is visible under `_POSIX_C_SOURCE`); bootstrap unchanged otherwise (`lab.py`, `asm/scalar.s`, `asm/unrolled4.s`, `src/reference.c`).

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 (Build 25G83), arm64, Python 3.14.7, Apple clang version 21.0.0 (clang-2100.3.34.2)
- Host string from lab: `macOS-26.6.2-arm64-arm-64bit-Mach-O`
- Revision or source hashes (SHA-256 at evidence time):
  - `asm/scalar.s`: `bece7ae6c51665ebfb795e44647afa534014fae5ad24b3b5c12f4f4328bf03f1`
  - `asm/unrolled4.s`: `31a1ed4514fc7cce6db5e167211f05980c89877b5d0ceb3186a2abb758ade9e6`
  - `src/reference.c`: `873cbe960a1726f0f20ed8b5358978fc3002a540e0f72a7c3c2774b698e5d595`
  - `src/harness.c`: `b4fcf68adf984e9cc488e79e532d0daf75d170cfca0619ef59b053a3e5c2dca5`
  - `lab.py`: `e43caa170fff13f6a4d5c6d6515206ee78a4c54393c60f2cf65c262626bdd6e6`
- Harness/contract hashes: harness hash above; kernel contract remains unsigned uint64 sum mod 2^64 via shared `sum_array` entry.

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 scripts/check_project.py` | 0 | PASS: packet and task bookkeeping | stdout |
| `python3 -m unittest discover -s tests -v` | 0 | 5 tests OK | stdout |
| `python3 lab.py doctor` | 0 | arm64 host + Apple clang reported | stdout |
| `python3 lab.py verify` | 0 | `clang_o3`, `scalar`, `unrolled4` all PASS | build harnesses under `build/` |
| `python3 lab.py bench --samples 15 --output results/initial.json` | 0 | All three variants timed for sizes 0,1,3,4,7,16,64,1024,65536,1048576; report saved | `results/initial.json` |

First `verify`/`bench` attempt failed (exit 1) before the harness fix: `CLOCK_MONOTONIC_RAW` undeclared when only `_POSIX_C_SOURCE 200809L` was set. After adding `_DARWIN_C_SOURCE`, both commands exited 0.

## Failure tests
Not required for T001 beyond built-in verify oracles. Intentional wrong-sum / memory / ABI fault fixtures are T003–T004 scope. Built-in harness self-check rejected mismatches via `expected()` before this task (unchanged).

## Limitations
- Bench timings are single-session observations under ambient load; not a performance-promotion claim (M003 protocol not implemented).
- T002 traceability (unique run dirs, full build argv capture, invalidate-on-edit) is not done; M001 bookkeeping remains incomplete until T002.
- No git revision recorded (workspace may lack meaningful commit identity); source SHA-256 used instead.

## Decision and next action
Pass. Native baseline established on supported Apple Silicon host.

Exact next command: begin T002 (`docs/IMPLEMENTATION_PLAN.md`) — make bootstrap artifacts traceable; start with inspecting `lab.py` build/verify paths and `templates/EVIDENCE.md` obligations for T002.
