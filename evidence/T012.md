# Evidence: T012 (revalidated)

## Change and acceptance mapping
- Task: T012 — Minimal `.ax` source + native template lowering, revalidated on verified kernel identities.
- Changes: `language/lower.py` resolves catalog asm via descriptors; `language/native.py` child-process isolation + return-size bounds; `language/SUBSET.md` updated.
- Disassembly: `evidence/disasm/T012_{sum_u64,find_u8,map_filter_u64}.txt`

## Environment
- macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0

## Executed checks

| Command | Exit | Result |
|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 134 OK (incl. language compile/native) |
| `python3 lab.py lang-check` | 0 | PASS n=3; isolated native matches interpreter |
| Disasm capture via `language.native.disassemble_template` | 0 | three files under `evidence/disasm/` |

## Acceptance mapping
- Source examples compile and run natively (child-isolated).
- Generated cases match interpreter.
- Return sizes for find/map_filter bounds-checked.
- Supported subset explicit in `language/SUBSET.md`.
- No Stage 4 concurrency/safety claims.

## Limitations
- Template catalog only (three shapes); no general codegen.
- Isolation covers native ctypes load in a child Python process, not a full sandbox.

## Acceptance
T012 / M006 revalidated.
