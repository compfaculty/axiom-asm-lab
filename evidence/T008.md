# Evidence: T008

## Change and acceptance mapping
- Task: T008 — Implement offline proposal pipeline (M004)
- Requirement/check: Validate proposal schema/limits/source review/provenance; mock proposer; immutable importer. One valid proposal completes build/verify/measure; malformed IDs, oversized source, includes, unknown kernels, invalid parents rejected.
- Implementation files: `proposals.py`, `lab.py` (`propose-mock`, `evaluate-proposal`), `tests/test_proposals.py`, `docs/TEST_FLOW.md`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git parent: `02fd0a7`
- SHA-256 at evidence time:
  - `proposals.py`: `9216eae20f1d73b31c7e141663acd5a0e7079149e50bd0e959353c1d0cf88601`
  - `lab.py`: `5630e7ba0f5b8da00a99f306918479689f08963759eb6e42e70b109e491107d9`
  - `tests/test_proposals.py`: `fdb9738b104a48910ca6efa60a443e34daced80c0ea532353ce2f9908adcf0bb`

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 53 tests OK (reject paths + immutable import) | stdout |
| `python3 lab.py propose-mock --output /tmp/mock_prop.json` | 0 | Valid schema_version=1 mock proposal | `/tmp/mock_prop.json` |
| `python3 lab.py evaluate-proposal --mock --samples 3 --output results/proposal_eval.json` | 0 | Import → build → verify/oracle → measure; lifecycle measured | `results/proposal_eval.json`, `build/runs/*/proposals/mock_scalar_copy/` |

## Failure tests
- Malformed `candidate_id`, unknown `kernel_id`, invalid parent sha256, oversized source, `.include`, unknown fields → `ProposalError`.
- Second import of same id into same run dir → `FileExistsError`.

## Limitations
- Online provider adapters deferred (T009 optional interface / later).
- Mock proposal is scalar-equivalent (pipeline proof), not a performance hypothesis search.

## Decision and next action
Pass.

Exact next command: begin T009 — bounded search and resume.
