# Evidence: T008

## Change and acceptance mapping
- Task: T008 — Offline proposal pipeline with shared evaluate path (M004)
- Requirement: Valid proposal completes build/verify/measure through the same pipeline as manual bench; malformed proposals rejected; protocol reports accepted by compare; smoke reports cannot promote.
- Implementation files: `evaluate.py`, `lab.py` (`evaluate-proposal --protocol|--smoke`), `proposals.py`, `tests/test_search_rank.py`, `tests/test_proposals.py`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git: `f8abcc98cdcb2903f72794cf1cb0f071e9a079bd`

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 109 tests OK | stdout |
| `python3 lab.py evaluate-proposal --mock --smoke --samples 3` | (covered by search smoke) | non-promotional measured | search smoke attempts |
| `python3 lab.py search --dir build/searches/smoke-e2e --smoke --provider catalog --max-proposals 2 --samples 3` | 0 | 2× stage=measured, accepted=null | `build/searches/smoke-e2e/` |
| `python3 lab.py search --dir build/searches/protocol-one --provider catalog --max-proposals 1 --samples 30` | 0 | ranked vs clang_o3, score≈0.53 | `build/searches/protocol-one/` |

## Failure tests
- Smoke/`promotional=False` reports rejected by `validate_report` (`test_smoke_report_rejected_by_validate`).
- Proposal schema/source review negatives remain in `tests/test_proposals.py`.

## Limitations
- Protocol evaluation is sum_u64 only.
- Smoke measurements are explicitly non-promotional and never set `accepted`.

## Decision and next action
Pass for T008 shared pipeline. Ranking/confirmation covered under T009.
