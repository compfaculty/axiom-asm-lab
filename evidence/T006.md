# Evidence: T006

## Change and acceptance mapping
- Task: T006 — Implement reproducible native sampling (M003)
- Requirement/check: Calibrate iterations; randomized paired order with seed; persist raw samples and host notes; memory caps and cache-mode labels. Calibration reaches target or reports cap; replay reproduces order; samples include iterations and elapsed time; memory caps enforced.
- Implementation files: `src/harness.c` (`calibrate`, `bench-raw`), `sampling.py`, `lab.py` (bench pipeline), `tests/test_sampling.py`, `docs/TEST_FLOW.md`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git parent: `7d28ea8`
- SHA-256 at evidence time:
  - `lab.py`: `0a99528c934e403c20fe6d4bb276d908df86718cbc4678ee7ffc5126cf958cbe`
  - `sampling.py`: `f194bc1797c00e07bd782724312fc4225d8aa3cc9e6343d7794d19e33027e5aa`
  - `src/harness.c`: `bebe393f19d7868c0264eab57ac1f7ca5a1fd8b9badfa88f92bae1edba013a3f`
- Bench artifact: `results/runs/20260923T223929_4fa40a4a/` (`bench.json` schema 2 + `samples.jsonl`)

## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 36 tests OK (schedule replay, memory cap, cache labels) | stdout |
| `python3 lab.py verify` | 0 | builtins PASS | run manifest |
| `python3 lab.py bench --samples 3 --seed 42 --target-sample-ms 5 --host-notes 'T006 evidence session'` | 0 | Calibrated paired sampling; raw samples with `iterations`+`elapsed_ns`; host notes; cache modes | `results/runs/20260923T223929_4fa40a4a/bench.json`, `samples.jsonl` |

## Failure tests
- `enforce_memory_cap` / `filter_sizes_for_cap` reject oversized `n` (portable).
- Same seed reproduces identical `paired_schedule`; different seed differs.

## Limitations
- Evidence bench used `--target-sample-ms 5` for runtime; protocol default remains 20 ms (`--target-sample-ms 20`).
- Cache-mode labels are logical footprint classes, not measured LLC residency.
- Promotion statistics remain T007.

## Decision and next action
Pass.

Exact next command: begin T007 — comparison report and paired bootstrap analysis.
