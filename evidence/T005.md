# Evidence: T005

## Change and acceptance mapping
- Task: T005 — Enforce verification before timing (M002)
- Requirement/check: Lifecycle states + artifact hashes; prevent measurement after failed/stale checks; retain diagnostics. Fault matrix passes; changed source cannot reuse a PASS; controller survives child faults; M002 evidence complete.
- Implementation files: `lab.py` (lifecycle, `assert_measurable`, diagnostics persistence), `tests/test_lifecycle.py`.

## Environment and provenance
- OS/CPU/toolchain: macOS 26.6.2 arm64, Python 3.14.7, Apple clang 21.0.0
- Git parent: `617b4e1`
- `lab.py` SHA-256: `bec64b034a65e5968e3f1cc6a9ce7b4b047bea30dca73ebb9ec32e0c548b1e85`
- `tests/test_lifecycle.py` SHA-256: `45362418c4fab605d51ec5a760a97b063e4bb82276a422f4b1a3a26c695a3c7c`
## Executed checks
| Command | Exit code | Observed result | Artifact |
|---|---|---|---|
| `python3 -m unittest discover -s tests -v` | 0 | 31 tests OK (lifecycle: stale PASS blocked, failed blocked, diagnostics persisted) | stdout |
| `python3 lab.py verify` | 0 | builtins PASS; records `lifecycle=verified` | `build/runs/20260923T223708_db468415/manifest.json` |
| `python3 lab.py fault-matrix` | 0 | 7/7 OK; failed fixtures get diagnostics; matrix continues after faults | `build/runs/20260923T223710_bf177d67/fault_matrix.json` |
| `python3 lab.py verify --candidate wrong_sum` | 1 | Rejected; diagnostics retained | `build/runs/.../diagnostics_wrong_sum.json` |

## Failure tests
- `assert_measurable` rejects `built` / `failed` / `stale` lifecycles.
- Editing source after verify marks `stale` and blocks reuse of PASS for timing.
- `wrong_sum` verify failure persists `diagnostics_wrong_sum.json`.

## Limitations
- Resume-across-process measurement of a prior run directory is not yet a first-class CLI (hashes/lifecycle are enforced within a run and via API); full search resume is T009.

## Decision and next action
Pass. M002 tasks T003–T005 complete.

Exact next command: begin T006 — reproducible native sampling (`docs/IMPLEMENTATION_PLAN.md`).
